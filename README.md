# 家谱小程序 UI 骨架

- 导入 `genealogy-weapp` 目录到微信开发者工具即可预览。
- 目前包含页面：`首页`、`家谱`、`成员详情`、`添加成员`、`动态`、`我的`。
- 全局样式令牌：`styles/tokens.wxss`。

## 运行
1. 打开微信开发者工具，选择导入项目。
2. AppID 可先用测试号（`touristappid`）。
3. 编译后即可在模拟器查看。

## 待办
- 接入组件库（WeUI/TDesign）以获得更一致的交互与适配。
- 家谱树视图可替换为自定义 Canvas/节点虚拟化实现。
